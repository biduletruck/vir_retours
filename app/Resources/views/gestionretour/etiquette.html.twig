<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
    <link rel="stylesheet" href="{{ asset('css/etiquette.css') }}">
</head>
<style type="text/css">

    .tg td{font-family:Arial, sans-serif;font-size:14px;padding:20px 20px;border-style:solid;border-width:3px;overflow:hidden;word-break:normal;}
    .tg th{font-family:Arial, sans-serif;font-size:14px;font-weight:normal;padding:20px 20px;border-style:solid;border-width:3px;overflow:hidden;word-break:normal;}
    .tg .tg-baqh{text-align:center;vertical-align:top}
    .tg .tg-9kdx{font-weight:bold;font-size:130px;font-family:"Arial Black", Gadget, sans-serif !important;background-color:#000000 !important;color:#ffffff !important;text-align:center}
    .tg .tg-9klht{font-weight:bold;font-size:100px;font-family:"Arial Black", Gadget, sans-serif;text-align:center}
    .tg .tg-9k div div{font-weight:bold;font-size:100px;font-family:"Arial Black", Gadget, sans-serif;background-color:black !important;text-align:center}
    .tg .tg-yw4l{vertical-align:top;text-align:center}

</style>
<body>

    <div class="container-fluid">
        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12">
                {% for i in 1 .. gestionRetour.nombreSupport %}
                    <table class="table tg">
                        <tr>
                            <th class="tg-9kdx" colspan="10">{{ gestionRetour.motifRetour.nomMotifRetour }}</th>
                        </tr>
                        <tr>
                            <td class="tg-9klht" colspan="10">{{ gestionRetour.nomDestinataire }}</td>
                        </tr>
                        <tr>
                            <td class="tg-9klht" colspan="10">{{ gestionRetour.donneurOrdre.nomDonneurOrdre }}</td>
                        </tr>
                        <tr>
                            <td class="tg-9k div div" colspan="7" rowspan="2">{{ codeSage |raw }}</td>
                            <td class="tg-9klht" colspan="3" rowspan="3">{{ i }}/{{ gestionRetour.nombreSupport }}</td>
                        </tr>
                        <tr>
                        </tr>
                        <tr>
                            <td class="tg-baqh" colspan="4">Numéro Sage: {{ gestionRetour.numeroSage }}</td>
                            <td class="tg-yw4l" colspan="3">Date d'entrée: {{ gestionRetour.dateEntreeEntrepot|date('d-m-Y') }}</td>
                        </tr>
                    </table>
                {% endfor %}
            </div>
        </div>
    </div>
</body>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
</html>

