{% extends 'base.html.twig' %}

{% block body %}
    <h1>Emplacement</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emplacement.id }}</td>
            </tr>
            <tr>
                <th>Nomemplacement</th>
                <td>{{ emplacement.nomEmplacement }}</td>
            </tr>
            <tr>
                <th>Codebarreemplacement</th>
                <td>{{ emplacement.codeBarreEmplacement }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_emplacement_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_emplacement_edit', { 'id': emplacement.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
