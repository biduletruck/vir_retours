{% extends 'base.html.twig' %}

{% block body %}
    <h1>Donneurordre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ donneurOrdre.id }}</td>
            </tr>
            <tr>
                <th>Nomdonneurordre</th>
                <td>{{ donneurOrdre.nomDonneurOrdre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_donneurordre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_donneurordre_edit', { 'id': donneurOrdre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
