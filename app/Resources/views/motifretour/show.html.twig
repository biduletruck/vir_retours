{% extends 'base.html.twig' %}

{% block body %}
    <h1>Motifretour</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ motifRetour.id }}</td>
            </tr>
            <tr>
                <th>Nommotifretour</th>
                <td>{{ motifRetour.nomMotifRetour }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_motifretour_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_motifretour_edit', { 'id': motifRetour.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
